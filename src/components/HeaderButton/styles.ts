import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  buttonPressed: {
    transform: [{scale: 0.96}],
  },
  buttonImage: {
    width: '100%',
    height: '100%',
    alignSelf: 'center',
  },
});
